---
phase: 05-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SlashCommands.lua
  - Data.lua
  - Events.lua
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "No InterfaceOptionsFrame_OpenToCategory calls remain in any Lua file"
    - "No bare LoadAddOn fallback remains (only C_AddOns.LoadAddOn)"
    - "No German debug strings remain in Events.lua"
    - "The only forward declaration in the codebase (Data.lua CancelApplicationAndMark) is a legitimate within-file mutual reference"
    - "All local functions use camelCase, all EFJ namespace exports use PascalCase, all constants use UPPERCASE"
  artifacts:
    - path: "SlashCommands.lua"
      provides: "Clean options-open path using only Settings API"
    - path: "Data.lua"
      provides: "Clean C_AddOns.LoadAddOn call without bare LoadAddOn fallback"
    - path: "Events.lua"
      provides: "English debug string"
  key_links: []
---

<objective>
Remove legacy API stubs, verify forward declarations are resolved, and confirm naming consistency across all 7 module files.

Purpose: Final cleanup phase — ensures no dead code from prior phases remains and the codebase follows consistent conventions.
Output: Clean SlashCommands.lua, Data.lua, Events.lua with legacy stubs removed; all other files confirmed clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

Phase 5 audit findings (gathered during planning):

QUAL-01 findings (legacy stubs):
1. SlashCommands.lua:16-18 — InterfaceOptionsFrame_OpenToCategory fallback. Dead on Midnight 12.x.
2. Data.lua:102 — `(C_AddOns and C_AddOns.LoadAddOn) or LoadAddOn` bare LoadAddOn fallback. Dead on 12.x.
3. Events.lua:109 — German debug string "Addon geladen. Initialisiere Aktualisierung." survived Phase 3 localization.

QUAL-02 findings (forward declarations):
1. Data.lua:92 — `local CancelApplicationAndMark` is a LEGITIMATE within-file forward declaration for mutual reference between TryJoinAndMark and CancelApplicationAndMark. No action needed.
2. No cross-file forward declarations exist. All inter-module calls use EFJ namespace.

QUAL-03 findings (naming conventions):
1. All naming is already consistent with CLAUDE.md conventions:
   - Local functions: camelCase (normalizeName, doApply)
   - EFJ exports: PascalCase (BuildSignature, ClassifyResult, etc.)
   - Constants: UPPERCASE (ROW_HEIGHT, MAX_ROWS, DEFAULTS, ADDON_NAME)
   - No violations found.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy API stubs (QUAL-01)</name>
  <files>SlashCommands.lua, Data.lua, Events.lua</files>
  <action>
Three targeted edits:

1. **SlashCommands.lua lines 16-19**: Remove the `InterfaceOptionsFrame_OpenToCategory` fallback block entirely. The function should only use the `Settings.OpenToCategory` path (lines 12-15). After the Settings block, if no category was found, do nothing (the options panel registers via Settings API in Events.lua, so this path always works on 12.x).

Current (remove):
```lua
  if InterfaceOptionsFrame and InterfaceOptionsFrame_OpenToCategory and EFJ.Options and EFJ.Options.panel then
    InterfaceOptionsFrame_OpenToCategory(EFJ.Options.panel)
    InterfaceOptionsFrame_OpenToCategory(EFJ.Options.panel)
  end
```

2. **Data.lua line 102**: Replace `(C_AddOns and C_AddOns.LoadAddOn) or LoadAddOn` with just `C_AddOns.LoadAddOn`. The addon targets 12.x exclusively where C_AddOns.LoadAddOn always exists.

Current: `local _LoadAddOn = (C_AddOns and C_AddOns.LoadAddOn) or LoadAddOn`
Replace: `local _LoadAddOn = C_AddOns.LoadAddOn`

3. **Events.lua line 109**: Replace German debug string with English.

Current: `DBG("Addon geladen. Initialisiere Aktualisierung.")`
Replace: `DBG("Addon loaded. Initializing refresh.")`
  </action>
  <verify>
    <automated>grep -rn "InterfaceOptionsFrame" *.lua; grep -rn "or LoadAddOn" *.lua; grep -rn "geladen" *.lua; echo "--- expect no output above ---"</automated>
  </verify>
  <done>
  - No InterfaceOptionsFrame references in any Lua file
  - No bare LoadAddOn fallback in any Lua file
  - No German debug strings remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify forward declarations and naming (QUAL-02, QUAL-03)</name>
  <files>Data.lua</files>
  <action>
Verification-only task with one minor documentation improvement:

1. **QUAL-02 verification**: Confirm Data.lua:92 `local CancelApplicationAndMark` is the ONLY forward declaration across all 7 files. This is legitimate — TryJoinAndMark (line 143) references CancelApplicationAndMark (line 164) which is defined after it. Mutual reference within a single file requires forward declaration in Lua. Add a clearer comment:

Current: `-- Forward declare for mutual reference`
Replace: `-- Forward declaration: TryJoinAndMark references CancelApplicationAndMark (defined below)`

2. **QUAL-03 verification**: Run grep to confirm no naming violations exist. Expected: all clean.

No other code changes needed — naming is already consistent per CLAUDE.md conventions.
  </action>
  <verify>
    <automated>echo "=== Forward declarations (expect only Data.lua CancelApplicationAndMark) ==="; grep -rn "^local [A-Z][a-zA-Z]*$" *.lua; echo "=== Bare _G references (expect none) ==="; grep -rn "_G\." *.lua; echo "=== Done ==="</automated>
  </verify>
  <done>
  - Only one forward declaration exists (Data.lua CancelApplicationAndMark) and it has an explanatory comment
  - No _G namespace pollution in any module file
  - Naming audit confirms consistency: camelCase locals, PascalCase EFJ exports, UPPERCASE constants
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This phase only removes dead code and updates comments.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T (Tampering) | SlashCommands.lua InterfaceOptions removal | accept | Removing dead code path; Settings API is the only active path on 12.x |
</threat_model>

<verification>
1. `grep -rn "InterfaceOptionsFrame" *.lua` returns no results
2. `grep -rn "or LoadAddOn" *.lua` returns no results
3. `grep -rn "geladen" *.lua` returns no results
4. `grep -rn "^local [A-Z]" *.lua` returns only Data.lua CancelApplicationAndMark
5. `grep -rn "_G\." *.lua` returns no results
</verification>

<success_criteria>
- No legacy fallback stubs remain (InterfaceOptions_AddCategory, activityID primary path, old LoadAddOn calls)
- All forward declarations that spanned sections in the monolith are resolved into the module where the function lives
- Function and variable names follow a consistent convention throughout all module files
- The only forward declaration is a legitimate within-file mutual reference with explanatory comment
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality/05-01-SUMMARY.md`
</output>
