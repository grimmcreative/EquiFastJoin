---
phase: 03-localization-system
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [EquiFastJoin.lua]
autonomous: true
requirements: [LOCA-05]

must_haves:
  truths:
    - "No hardcoded German UI strings remain in EquiFastJoin.lua outside the L-table deDE block"
    - "enUS client sees English strings for all buttons, labels, errors, and options"
    - "deDE client sees the same German strings as before (no regression)"
    - "Slash command output is in English"
  artifacts:
    - path: "EquiFastJoin.lua"
      provides: "All UI strings served via L[\"key\"] calls"
      contains: "L[\"Join\"]"
  key_links:
    - from: "L[\"key\"] callsites"
      to: "L-table definition (from Plan 01)"
      via: "local L upvalue"
      pattern: 'L\["'
---

<objective>
Replace every hardcoded German UI string in EquiFastJoin.lua with its corresponding L["key"] call. Also update the German slash command help text to English per the locked decision that slash commands stay English.

Purpose: Complete the localization — after this plan, the addon serves locale-appropriate strings to all users.
Output: EquiFastJoin.lua with zero hardcoded German UI strings outside the L-table deDE block and DBG calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-localization-system/03-RESEARCH.md
@.planning/phases/03-localization-system/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded German strings with L["key"] calls</name>
  <files>EquiFastJoin.lua</files>
  <read_first>EquiFastJoin.lua (full file — need to locate all string callsites after L-table insertion shifted line numbers)</read_first>
  <action>
Replace every hardcoded German string listed below with its L["key"] equivalent. The L-table was inserted by Plan 01, so line numbers have shifted — use the string content to locate each callsite, not hardcoded line numbers.

**IMPORTANT:** Do NOT modify the L-table deDE block itself. Do NOT modify DBG() call strings. Do NOT modify the slash command debug output strings ("EFJ: Debug an", "EFJ: Debug aus").

**Button labels — replace these exact strings at their callsites:**

| Find (exact string in SetText/AddMessage) | Replace with |
|-------------------------------------------|-------------|
| `"Beitreten"` (6 occurrences: CreateRow, UpdateJoinButton default, MarkAppliedByID reset, ShowQuickJoin lfgID, SetRows default) | `L["Join"]` |
| `"Abmelden"` (3 occurrences: TryJoinAndMark after apply, UpdateJoinButton applied, MarkAppliedByID applied) | `L["Leave"]` |
| `"Eingeladen"` (2 occurrences: UpdateJoinButton invited, MarkAppliedByID invited) | `L["Invited"]` |
| `"Abgemeldet"` (1 occurrence: CancelApplicationAndMark after cancel) | `L["Left"]` |
| `"Nicht LFG"` (1 occurrence: ShowQuickJoin no lfgID) | `L["Not in LFG"]` |
| `"OK"` (1 occurrence: ShowBanner dismiss button) | `L["OK"]` |

**Activity/content strings:**

| Find | Replace with |
|------|-------------|
| `"Unbekannte Aktivität"` (3 occurrences: BuildActivityText nil res, BuildActivityText fallback, SetRows fallback) | `L["Unknown Activity"]` |
| `"Neuer Eintrag"` (1 occurrence: ToastForIDs fallback) | `L["New Entry"]` |
| `"Schnellbeitritt"` (2 occurrences: ShowBanner headline default, GatherQuickJoinEntries activityText fallback) | `L["Quick Join"]` |

**Banner/test strings:**

| Find | Replace with |
|------|-------------|
| `"Es sind neue Schnellbeitritt-Vorschläge verfügbar."` (1 occurrence: ShowBanner subline default) | `L["New Quick Join suggestions available."]` |
| `"Dies ist ein Testeintrag."` (1 occurrence: ShowTest subline) | `L["This is a test entry."]` |

**Error messages (UIErrorsFrame:AddMessage calls):**

| Find | Replace with |
|------|-------------|
| `"EFJ: Beitritt im Kampf gesperrt"` | `L["EFJ: Join blocked in combat"]` |
| `"EFJ: Bewerbung fehlgeschlagen"` | `L["EFJ: Application failed"]` |
| `"EFJ: Aktion blockiert (Taint)"` | `L["EFJ: Action blocked (Taint)"]` |
| `"EFJ: Abmelden im Kampf gesperrt"` | `L["EFJ: Leave blocked in combat"]` |
| `"EFJ: Abmelden fehlgeschlagen"` | `L["EFJ: Leave failed"]` |
| `"EFJ: Nicht im Kampf verfügbar"` | `L["EFJ: Not available in combat"]` |
| `"EFJ: Optionen im Kampf gesperrt"` | `L["EFJ: Options blocked in combat"]` |

**Options panel (EFJ.Options:Create):**

| Find | Replace with |
|------|-------------|
| `"EquiFastJoin Optionen"` | `L["EquiFastJoin Options"]` |
| `"Dungeons anzeigen"` | `L["Show Dungeons"]` |
| `"Zeigt Dungeon-Gruppen"` | `L["Show dungeons"]` |
| `"Raids/Schlachtzüge anzeigen"` | `L["Show Raids"]` |
| `"Zeigt Raid-Gruppen"` | `L["Show raids"]` |
| `"Mythic+ anzeigen"` | `L["Show Mythic+"]` |
| `"Zeigt M+ Gruppen"` | `L["Show M+ groups"]` |
| `"PvP anzeigen"` | `L["Show PvP"]` |
| `"Zeigt PvP-Gruppen"` | `L["Show PvP groups"]` |
| `"Benutzerdefiniert/Quest anzeigen"` | `L["Show Custom/Quest"]` |
| `"Zeigt benutzerdefinierte/Quest-Gruppen"` | `L["Show custom/quest groups"]` |
| `"Bei Schnellbeitritt auto-öffnen"` | `L["Auto-open on Quick Join"]` |
| `"Öffnet Liste bei QuickJoin Vorschlägen"` | `L["Opens list on Quick Join suggestions"]` |
| `"Sound abspielen"` | `L["Play Sound"]` |
| `"Spielt einen kurzen Sound beim Öffnen"` | `L["Plays a short sound on open"]` |
| `"Toast Nachricht"` | `L["Toast Message"]` |
| `"Zeigt eine RaidWarning Toast"` | `L["Shows a RaidWarning toast"]` |
| `"Rahmen sperren"` | `L["Lock Frame"]` |
| `"Verhindert Verschieben des Fensters"` | `L["Prevents moving the window"]` |
| `"Skalierung"` | `L["Scale"]` |
| `"Testfenster"` | `L["Test Window"]` |
| `"Jetzt aktualisieren"` | `L["Refresh Now"]` |

**Slash command help (update German to English):**

| Find | Replace with |
|------|-------------|
| `"EFJ: Verwende /efj test \| show \| hide \| options \| debug on\|off"` | `"EFJ: Usage: /efj test \| show \| hide \| options \| debug on\|off"` |

Note: This slash command string is NOT wrapped in L["key"] — per the locked decision, slash commands stay single-language (English). Also update `"EFJ: Debug an"` to `"EFJ: Debug on"` and `"EFJ: Debug aus"` to `"EFJ: Debug off"` for consistency with the English slash command convention.

**Do NOT touch:**
- The L-table deDE block (those strings are the translation data)
- DBG() call arguments (developer-facing)
- `"Mythic+"` on the line with `return "Mythic+"` (brand name, not translatable)
- `("Mythic+ +%d"):format(level)` (brand name in format string)
  </action>
  <verify>
    <automated>cd /Users/grimm/DEVELOPEMENT/EquiFastJoin && echo "=== German strings outside L-table ===" && awk '/^if locale == "deDE" then$/,/^end$/{next} /DBG\(/{next} {print NR": "$0}' EquiFastJoin.lua | grep -iE '"[^"]*[äöüÄÖÜß][^"]*"' | grep -v "Schlachtz" | head -20; echo "=== Button label check ===" && grep -n '"Beitreten"\|"Abmelden"\|"Eingeladen"\|"Abgemeldet"' EquiFastJoin.lua | grep -v 'L\[' | grep -v 'locale.*deDE' | head -10; echo "=== L-key usage count ===" && grep -c 'L\["' EquiFastJoin.lua</automated>
  </verify>
  <acceptance_criteria>
- `grep -n '"Beitreten"' EquiFastJoin.lua` returns ONLY lines inside the L-table deDE block
- `grep -n '"Abmelden"' EquiFastJoin.lua` returns ONLY lines inside the L-table deDE block
- `grep -n '"Eingeladen"' EquiFastJoin.lua` returns ONLY lines inside the L-table deDE block
- `grep -n '"Unbekannte Aktivität"' EquiFastJoin.lua` returns ONLY lines inside the L-table deDE block
- `grep -n '"Schnellbeitritt"' EquiFastJoin.lua` returns ONLY lines inside the L-table deDE block
- `grep -n '"EquiFastJoin Optionen"' EquiFastJoin.lua` returns ONLY lines inside the L-table deDE block
- `grep -c 'L\["' EquiFastJoin.lua` returns >= 70 (35 deDE entries + ~35 callsite usages)
- `grep '"Verwende"' EquiFastJoin.lua` returns 0 matches (German help text replaced)
- `grep '"EFJ: Debug on"' EquiFastJoin.lua` returns 1 match
- `grep '"EFJ: Debug off"' EquiFastJoin.lua` returns 1 match
- No German UI strings (containing ä, ö, ü, ß) appear outside the deDE block and DBG calls
  </acceptance_criteria>
  <done>All hardcoded German UI strings replaced with L["key"] calls. Slash command output updated to English. Zero German strings remain outside the L-table deDE block and DBG calls. enUS clients will see English, deDE clients will see German via the L-table overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Addon file → WoW Lua VM | No new trust boundaries; string replacement is internal refactoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | I (Information Disclosure) | L-table key strings | accept | Keys are English UI labels — no sensitive data. Visible to users as intended. |
</threat_model>

<verification>
Final verification after all replacements:

```bash
# 1. No German button labels outside deDE block
grep -n '"Beitreten"\|"Abmelden"\|"Eingeladen"\|"Abgemeldet"' EquiFastJoin.lua | grep -v "deDE" | wc -l
# Expected: 0

# 2. No German error messages outside deDE block
grep -n '"EFJ: Beitritt\|"EFJ: Bewerbung\|"EFJ: Aktion\|"EFJ: Abmelden\|"EFJ: Nicht\|"EFJ: Optionen' EquiFastJoin.lua | grep -v "deDE" | wc -l
# Expected: 0

# 3. L["key"] usage count
grep -c 'L\["' EquiFastJoin.lua
# Expected: >= 70

# 4. Slash command output is English
grep -c '"Verwende"' EquiFastJoin.lua
# Expected: 0
```
</verification>

<success_criteria>
- Every hardcoded German UI string from the research inventory is replaced with L["key"]
- Slash command help text updated from German to English
- Debug output strings ("Debug an"/"Debug aus") updated to English ("Debug on"/"Debug off")
- No German strings remain outside the L-table deDE block and DBG calls
- Total L["key"] callsite count is approximately 35+ (matching the 35 distinct keys)
</success_criteria>

<output>
After completion, create `.planning/phases/03-localization-system/03-02-SUMMARY.md`
</output>
